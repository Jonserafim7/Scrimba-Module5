setTimeout - controls the time for when a given function should be triggered;

element.style - allows to dinamically controlling the style of html elements using js;

forms - forms are used to ask users to input data; in this example we used the input types text, email and password;

formData & .get() - the first is used to store the data the user inputs in the form; 
                    the second is used to get the data that has been stored;

event.preventDefault - more control on how we submit the form by preventing the default behavior

CSS: row-reverse

toggling classes

"disabled" attribute